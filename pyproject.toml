[project]
name = "clubwpt-scraper"
version = "0.1.0"
description = "Scrape ClubWPT Gold hand history and convert to PokerStars format"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "pytest>=9.1.1",
]
